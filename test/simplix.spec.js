const { SimplixExpress } = require('../src/simplix');
const { describe, it } = require('express');

describe('Simplix', () => {
    it('should handle get request', async () => {
        const simplix = new SimplixExpress();
        
    });
});